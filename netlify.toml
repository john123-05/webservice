[build]
  publish = "dist"

# Serve .html files at clean URLs (pretty URLs)
[[redirects]]
  from = "/*.html"
  to = "/:splat"
  status = 301
  force = false

# Rewrite clean URLs to actual .html files
[[redirects]]
  from = "/:page"
  to = "/:page.html"
  status = 200
  force = false
